{
  "name": "front",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "12.x.x",
    "yarn": ">=1.0.0 <2.0.0"
  },
  "scripts": {
    "start": "serve -s build",
    "prestart": "yarn global add serve"
  }
}